__default__:
        job-name : "{rule}"
        queue : "agap_short"
        nodes : "--nodes=1 "
        cpus : "--cpus-per-task=1"
        time : "--time=1:00:00"
        out : "--output={params.clust_log}"
        mem : ""

cleanReads:
        mem : "--mem=50G"

BWAmemRef:
        queue : "agap_short"
        cpus : "--cpus-per-task=2"
        mem : "--mem=10G"
        
samtoolsConsensus:
        queue : "agap_normal"
        time : "--time=5:00:00"
        mem : "--mem=10G"

BWAmemCons:
        cpus : "--cpus-per-task=2"
        mem : "--mem=10G"

samtoolsConsensus2:
        queue : "agap_normal"
        time : "--time=5:00:00"
        mem : "--mem=10G"

