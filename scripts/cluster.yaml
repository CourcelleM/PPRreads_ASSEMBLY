__default__:
        job-name : "{rule}"
        queue : "fast"
        nodes : "--nodes=1 "
        cpus : "--cpus-per-task=1"
        time : "--time=1:00:00"
        out : "--output={params.clust_log}"
        mem : ""

BWAmemRef:
        queue : "fast"
        
samtoolsConsensus:
        queue : "long"
        time : "--time=5:00:00"
        mem : "--mem 10G"

samtoolsConsensus2:
        queue : "long"
        time : "--time=5:00:00"
        mem : "--mem 10G"

